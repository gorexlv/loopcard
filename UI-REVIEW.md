# LoopCard Web 产品设计审查

审查日期：2026-09-05  
审查范围：首页、Market、Market 卡包详情、登录后卡包库、建包/编辑、Web 学习体验  
审查依据：当前 React/Next.js 实现与 `docs/afterimage-qa/final/` 中的桌面、移动端截图

## 结论

当前 Web 端有明确的视觉方向，但产品价值被“杂志式品牌叙事”压住了。用户在首屏看见一句抽象口号，随后要阅读多个大字号章节，直到很后面才接触卡片；即使看到卡片，首页上的翻卡和评分控件也只是静态展示，不能完成一次真实复习。

问题不是缺少页面，而是核心任务没有成为页面骨架。Web 端应围绕一条可在 30 秒内完成的主路径重组：

> 选择卡包 → 看问题 → 心中作答 → 翻卡 → 评分 → 看到下一张

当前总体评分：**13 / 24（需要结构性优化）**

| 维度 | 评分 | 判断 |
|---|---:|---|
| 文案 Copywriting | 2 / 4 | 有品牌调性，但抽象、重复，功能价值出现太晚 |
| 视觉 Visuals | 3 / 4 | 首页质感较强，但产品界面和品牌页割裂 |
| 色彩 Color | 3 / 4 | 色彩克制且辨识度高，但学习页整体过暗、状态含义依赖颜色 |
| 字体 Typography | 2 / 4 | 大标题有风格，但信息密度与字号层级失衡，中文适配不足 |
| 间距 Spacing | 2 / 4 | 留白漂亮，但过度留白延长任务路径，桌面学习页空间利用率低 |
| 体验设计 Experience | 1 / 4 | 核心体验不可立即试用，路径断裂，创建方式不符合“卡片”心智 |

## P0：先修用户旅程

### 1. 首屏主 CTA 没有兑现“立即体验”

- 证据：`AnimatedHero` 的 “Start a loop” 直接跳到 `/app`，通常先触发登录，而不是开始一次卡片体验。
- 影响：新用户还不知道产品是否好用，就先被要求建立账户；首屏承诺的是“开始一轮”，实际发生的是“进入账户系统”。
- 建议：主 CTA 改为 **“试学 5 张卡”**，直接进入无需登录的演示学习页；次 CTA 才是“创建我的卡包”。完成 5 张后再提示保存进度或注册。

### 2. 首页最像产品的区域是假的交互

- 证据：`HomePage` 中 `memory-card-scene` 的卡片和三个评分按钮没有状态和事件处理。
- 影响：界面看起来可点击却没有反应，直接损害可信度；用户无法从首页理解 recall → reveal → rate 的真实手感。
- 建议：复用 `StudyExperience` 的轻量逻辑，把这一段改成真正可翻卡、可评分、可进入下一张的 3 卡 demo。删除旁边大段解释，让交互本身解释产品。

### 3. Market 详情能翻卡，却不能直接开始学习

- 证据：详情页主动作只有 “Add to my decks / Sign in to add”；交互预览支持前后翻和选卡，但不提供“开始这一组”的连续复习入口。
- 影响：发现内容 → 体验内容 → 收藏内容的自然顺序被反转成先登录收藏，再体验完整流程。
- 建议：增加 **“立即学习”**（无需登录）为主按钮，“保存到我的卡包”降为次按钮。学习完成后再引导保存。

### 4. 创建卡包仍是文本导入器，不是卡片编辑器

- 证据：建包页使用一个 14 行 textarea，要求用户记住 `front :: back` 语法；没有卡片预览、逐卡编辑、错误定位或即时反馈。
- 影响：这满足批量数据录入，却没有提供“制作一张好卡”的基本体验，也让非技术用户感到产品粗糙。
- 建议：默认提供可视化卡片编辑器：正面、背面、补充说明、标签；支持添加/删除/拖动排序和实时预览。把 `front :: back` 保留为“批量粘贴”高级入口。

## P1：重组信息架构与页面职责

### 推荐的核心导航

登录前：**体验卡片 / 卡包市场 / 制作卡片**  
登录后：**今天复习 / 我的卡包 / 发现 / 新建**

现在的 “Market / Tools / Journal / Open LoopCard” 同时混合了内容、工具、媒体和账户动作，无法告诉用户最主要的三个任务是什么。`Journal` 对首次用户的重要性明显低于“学习”和“制作”。

### 推荐首页顺序

1. 首屏：一句具体价值 + 一张可操作卡片 + “试学 5 张卡”
2. 三步演示：看问题 → 翻答案 → 评价记忆
3. 可直接学习的热门卡包（不要先展示平台统计）
4. 创建方式：手动建卡 / 粘贴笔记 / 从图片导入
5. 登录与跨端同步价值

当前首页包含 6 个大章节，叙事顺序是理念 → 方法 → 模拟体验 → 场景 → 指标 → 精选 → 创建。它适合品牌展示，但不适合工具型产品的首次使用。建议压缩为 3 个屏幕左右，把解释性文字减少约 50–65%。

### 推荐首次使用路径

```text
首页可操作卡片
  → 完成 3–5 张匿名练习
  → 展示本轮结果
  → 选择：继续学习 / 保存卡包 / 创建自己的卡
  → 仅在需要跨设备保存时登录
```

## 六维详细审查

### 文案：2 / 4

优点：品牌语气统一，`recall / reveal / rate` 是清晰的核心模型。

问题：

- “What you recall becomes yours”“A living index of things worth recalling”有文学性，但不能在 5 秒内回答“这是一个能创建、管理和复习卡片的 Web 应用”。
- 首页连续使用超大标题和说明段落，视觉上像很多个 Hero，用户需要阅读而不是操作。
- 中文模式并不完整：学习页依据卡包内容猜测语言，Market 和编辑器仍以英文为主；“门清儿 / 搂一眼”地域化过强，不适合作为默认中文评分标签。
- “Start a loop”“Open LoopCard”“Create a deck”对同类动作使用了不同表述，增加理解成本。

建议统一任务语言：**开始学习、翻看答案、记住了、有点模糊、没记住、创建卡包、保存卡包**。

### 视觉：3 / 4

优点：首页的影像、纸张质感、深蓝与青绿色形成了独特品牌识别，Market 的卡片正反面预览比普通列表更接近产品本体。

问题：

- 品牌页是高端编辑设计，工作台却是传统侧栏 + 三列信息卡，建包页是普通表单，学习页又像独立移动 App；视觉语言在核心路径中断裂。
- Market 首屏最大的元素是标题与统计，不是卡包；用户要滚动后才开始“逛卡”。
- 大量细线、极小 uppercase 标签和低对比辅助文字更像杂志排版，不利于快速扫描任务和状态。

### 色彩：3 / 4

优点：暖白、煤蓝、青绿构成稳定，主色使用克制。

问题：

- Web 学习页大面积深色且对比偏低，侧栏品牌、标题和底部同步文字接近不可见。
- 三种评分动作主要依赖青/黄/红底线区分；应同时用清晰标签、图标或快捷键提示表达含义。
- 市场、工作台、学习页的表面色和亮暗策略不统一，造成“跳到另一个产品”的感觉。

### 字体：2 / 4

优点：衬线展示字体建立了鲜明品牌气质。

问题：

- 54–104px 的标题在多个连续区块重复，所有章节都在争夺最高注意力。
- 超大衬线标题挤占可操作内容，Market 首屏和首页尤其明显。
- 小字号全大写标签、细字辅助信息在高分辨率截图中仍难扫读。
- 中文字体与英文字体的气质和字重匹配不足，英文斜体装饰在中文页面没有等价表达。

建议把展示型超大字号限制在首页首屏一处；功能页以 32–48px 页面标题、16px 正文、14px 辅助文本为主。

### 间距：2 / 4

优点：区块边界清楚，整体不拥挤。

问题：

- 首页多个区块使用 90–170px 垂直 padding，导致用户在看到真正卡包之前经历过长滚动。
- Market 首屏标题、统计和搜索占据近一整个视口，卡包内容被推到折叠线以下。
- 学习页在桌面仅使用约 390px 宽的“手机壳”，其余空间成为没有信息价值的暗背景。
- 我的卡包每张卡片高度较大但信息很少，列表密度低，卡包封面/首卡/进度等更有用信息缺失。

### 体验设计：1 / 4

问题：

- 新用户不能在首页完成一次真实学习循环。
- 登录被放在价值验证之前。
- 评分后直接进入下一张，没有撤销、快捷键、进度解释或“为什么它会再出现”的反馈。
- 桌面学习页没有队列、卡包概览、快捷键提示、退出/返回卡包等桌面优势。
- 卡片收藏状态只存在组件内存中，页面刷新即丢失，容易造成错误预期。
- 完成页只显示复习张数，不显示记住/模糊/忘记分布，也没有明确的下一步。
- 建包后的编辑结构把丰富的卡背 `sections` 压缩成单一字符串，和 Market 中展示的分层卡背模型不一致。

## 目标页面草图

### 桌面首页首屏

```text
┌────────────────────────────────────────────────────────────┐
│ LoopCard     卡包市场   制作卡片                 登录       │
├───────────────────────────┬────────────────────────────────┤
│ 把任何内容                │  试一张卡                      │
│ 变成真正记得住的卡片      │                                │
│                           │  什么是机会成本？              │
│ 创建、复习并持续巩固      │                                │
│ 你的知识卡包。            │       [翻看答案]               │
│                           │                                │
│ [试学 5 张卡] [创建卡包]  │  1 / 5                         │
└───────────────────────────┴────────────────────────────────┘
```

### 桌面学习页

```text
┌───────────────┬──────────────────────────────┬──────────────┐
│ ← 我的卡包    │ 卡包名                 8/20 │ 本轮          │
│ 今日待复习 20 │                              │ 记住  5       │
│               │       一张宽大的卡片         │ 模糊  2       │
│ 卡片列表/队列 │                              │ 忘记  1       │
│               │ [空格 翻面]                 │ 快捷键提示    │
│               │ [1 记住][2 模糊][3 没记住] │              │
└───────────────┴──────────────────────────────┴──────────────┘
```

移动端保留单卡沉浸模式；桌面端不要继续模拟手机外壳，而应利用横向空间提供学习上下文和进度。

## 建议执行顺序

1. **可试玩闭环**：匿名 demo、首页真实翻卡、Market 直接学习、完成结果页。
2. **学习页重构**：响应式桌面布局、清晰评分、键盘操作、撤销和结果反馈。
3. **建卡重构**：可视化逐卡编辑 + 批量粘贴作为辅助能力。
4. **首页减法**：删除重复理念段落，降低大标题数量，让卡片和卡包提前出现。
5. **统一产品壳层**：统一导航、术语、色彩、字体层级与登录前后路径。
6. **本地化与状态**：完整中英文、空状态、错误、加载、保存反馈和持久化收藏。

## 验收指标

- 未登录用户从首页到完成第一张卡：最多 1 次点击。
- 未登录用户完成一轮 5 张卡：不要求注册。
- 首屏同时回答三个问题：这是什么、能做什么、现在能做什么。
- 首页首个视口内存在可操作卡片，而不是仅有口号与背景图。
- Market 首屏能看到至少 3 个真实卡包或一个明确的学习入口。
- 桌面学习区使用主内容宽度的至少 60%，并提供键盘快捷键。
- 创建第一张卡无需学习任何分隔符语法。
- 首页正文总量减少至少 50%，但核心流程无需文字说明也能理解。
