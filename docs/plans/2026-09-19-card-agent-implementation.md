# Card Agent 实施计划

- [x] 多图批次：逐张拍摄、重拍、补拍、删除、识别失败重试、内容校对与来源。
- [x] 对话服务：注册单词/知识问答 Skill，结构化方案配置、对话历史、生成及验证。
- [x] 移动端会话：账号隔离的本地恢复、方案推荐、消息内双面卡片、草稿编辑与确认。
- [x] 保存：保存生成规则与排版，幂等保存，学习页复用预览渲染。
- [x] 验证：Dart/Flutter、Deno、数据库事务与权限测试。

实现决策：首批最多 10 张照片，素材文字总长最多 4000 字，单次最多 30 张卡片。
照片仅设备端识别，提交 OCR 文本与照片来源标识；不上传原图。未完成生成会话保存在当前账号的本机空间。


验证记录（2026-09-19）：

- Flutter 全量回归 106 项通过；随后新增会话编辑/预览与手机截图测试，相关测试集 12 项通过；多图拍摄交互端到端测试 1 项通过。
- Deno 服务端测试 17 项通过，Edge Function 类型检查通过。
- 本地数据库 49 项 pgTAP 测试通过，security advisor 未报告问题。
- 本地 Card Agent 启动成功，实测未认证请求 401、非法请求 400、未配置模型服务 503。
- 正反面手机截图位于 `apps/mobile/test/goldens/20-agent-chat-front.png` 和 `21-agent-chat-back.png`。

运行边界：已应用迁移到项目 CLI 本地数据库（54322），未部署线上或改动独立的 8000 端口服务。
仓库没有模型 API 密钥，因此真实模型输出的联调尚未完成；真机相机/裁剪仍需设备验收。
已有设备端 Latin OCR 能力保持不变。首次选方案展示明确标注的排版示例；对话可返回一张基于素材的方案预览，整批生成后才开放保存。
