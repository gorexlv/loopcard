# 诗词、古文 Skill 接入验证

新增 poetry-card-generation 和 classical-chinese-card-generation 两份运行时 Skill；服务注册、结构化输出、移动端选项与静态示例、卡片类型映射和保存 RPC 同步支持。新迁移仅应用本地数据库，未部署远程服务。

| 验证项 | 结果 | 证据边界 |
|---|---|---|
| 两份 SKILL.md 格式验证 | 通过 | skill-creator quick_validate |
| Deno 校验与回归 | 21 通过 | 包含四种文学方案、聊天切换、原文字符串保留、来源保留、规则不一致及非法词汇字段拒绝；固定输入，并非模型质量评分 |
| 数据库回归 | 62 通过 | 新增 13 项：四方案保存、元数据、重复保存幂等、Skill/preset 不匹配拒绝 |
| Flutter 文学卡渲染及会话测试 | 通过 | 覆盖正反面、恢复、选择方案、示例与生成草稿的区分；使用固定样本 |
| Flutter analyze / Deno check | 通过 | 静态检查 |
| 本地 Edge Function HTTP 冒烟 | 3 通过 | 未认证拒绝、非法输入拒绝、加载 Skill 后明确返回未配置模型；临时账号已删除 |
| 实际模型问答 | 未完成 | 前次真实调用发现 API Key 无效，本轮没有可用凭证；不能据此宣称翻译、作者核对、多音字回答质量通过 |

已更新真实问答测试脚本和案例：默认单词入口转诗词/古文、诗词接句、作者冲突、古文字词、全文缺失与不支持的音频/动画。获得有效服务端模型配置后运行 `supabase/tests/card-agent/run_scenarios.py`，通过 `--output` 指定新文件，保留前次失败记录。

生成原则：以用户素材为原文依据，不静默纠错或补全文；原文与解释分区；古义、读音、通假字结合上下文；缺失或冲突先澄清。提示词约束仍需真实模型多轮测试确认遵守程度。
