AI_PROVIDER=deepseek
AI_MODEL=deepseek-flash
DEEPSEEK_API_KEY=
# OpenAI remains available as a fallback:
# AI_PROVIDER=openai
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Optional CardAgent/word generation via OpenRouter (explicit opt-in):
# AI_PROVIDER=openrouter
# AI_MODEL=qwen/qwen3-vl-235b-a22b-instruct

# Photo OCR (server only)
OPENROUTER_API_KEY=
OCR_MODEL=qwen/qwen3-vl-235b-a22b-instruct

# Automatic profile portraits reuse OPENROUTER_API_KEY above.
# Without it, OPENAI_API_KEY is used instead (model: gpt-image-2).
# AVATAR_IMAGE_MODEL=openai/gpt-image-2
